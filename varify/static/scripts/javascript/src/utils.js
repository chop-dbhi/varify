// Generated by CoffeeScript 1.7.1
define([], function() {
  var depthClass, effectImpactPriority, getRootUrl, priorityClass, qualityClass, toAbsolutePath;
  getRootUrl = function() {
    return window.location.href.replace(new RegExp('/[^/]*/$'), '/');
  };
  toAbsolutePath = function(path) {
    return "" + (getRootUrl()) + path;
  };
  effectImpactPriority = function(impact) {
    switch (impact) {
      case 'High':
        return 1;
      case 'Moderate':
        return 2;
      case 'Low':
        return 3;
      case 'Modifier':
        return 4;
      default:
        return 5;
    }
  };
  priorityClass = function(priority) {
    switch (priority) {
      case 1:
        return 'text-error';
      case 2:
        return 'text-warning';
      default:
        return '';
    }
  };
  depthClass = function(depth) {
    if (depth < 10) {
      return 'text-warning';
    } else if (depth >= 30) {
      return 'text-success';
    } else {
      return '';
    }
  };
  qualityClass = function(qual) {
    if (qual < 30) {
      return 'text-warning';
    } else if (qual >= 50) {
      return 'text-success';
    } else {
      return '';
    }
  };
  return {
    getRootUrl: getRootUrl,
    toAbsolutePath: toAbsolutePath,
    effectImpactPriority: effectImpactPriority,
    priorityClass: priorityClass,
    depthClass: depthClass,
    qualityClass: qualityClass
  };
});
